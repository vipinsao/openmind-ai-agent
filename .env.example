# Copy to .env.local:  cp .env.example .env.local
#
# Google AI Studio key — free tier, no credit card:
#   https://aistudio.google.com/app/apikey
# The route accepts GOOGLE_API_KEY as well, because that is what this project's
# hosting environment was originally configured with.
GEMINI_API_KEY=

# Optional. Defaults to gemini-3.6-flash.
# gemini-1.5-flash-latest and gemini-2.0-flash have both been retired from this
# endpoint. If the default ever 404s, the error the route returns names the
# replacement.
GEMINI_MODEL=
